# dependencies
node_modules

# build output
dist
dist-ssr
dist.zip

# env (סודות — לא נכנסים ל-git)
.env
.env.local
.env.*.local

# logs
*.log
npm-debug.log*

# editor / os
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db

# כלי Claude Code מקומיים — לא שייכים לרפו הציבורי (סימלינקים עם נתיב מקומי)
.agents/
.claude/
skills-lock.json

# קבצי zip זמניים
*.zip
